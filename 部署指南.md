# 🚀 五子棋游戏部署指南

## 📋 部署前准备

### 1. 本地测试完成
确保在本地环境已经成功运行：
- ✅ 服务器可以正常启动（端口 5000）
- ✅ 客户端可以正常启动（端口 3000 或 3001）
- ✅ 可以创建房间和加入房间
- ✅ 游戏可以正常进行

### 2. 服务器要求
- Node.js v14+ 
- npm 或 yarn
- 足够的磁盘空间（至少 500MB）
- 开放端口（默认 5000，可自定义）

---

## 🌐 部署到服务器

### 方案1：单服务器部署（推荐）

#### 步骤1：上传项目文件
```bash
# 使用 scp 或 FTP 工具上传项目到服务器
# 建议上传到 /var/www/gomoku-online 或类似目录
```

#### 步骤2：在服务器上安装依赖
```bash
# SSH 连接到服务器
ssh user@your-server.com

# 进入项目目录
cd /var/www/gomoku-online

# 安装服务器依赖
cd server
npm install --production

# 安装客户端依赖
cd ../client
npm install
```

#### 步骤3：构建客户端
```bash
cd client
npm run build
```

#### 步骤4：配置生产环境
创建 `server/.env` 文件：
```env
NODE_ENV=production
PORT=5000
```

#### 步骤5：配置客户端连接地址
修改 `client/src/components/Join.js` 和 `Lobby.js`：
```javascript
// 将服务器地址改为你的服务器IP或域名
const ENDPOINT = process.env.NODE_ENV === 'production' 
    ? 'http://your-server-ip:5000'  // 或 'https://your-domain.com'
    : 'http://localhost:5000';
```

#### 步骤6：重新构建客户端
```bash
cd client
npm run build
```

#### 步骤7：配置服务器提供静态文件
修改 `server/index.js`，添加静态文件服务：
```javascript
const express = require('express');
const path = require('path');

// ... 其他代码 ...

// 提供静态文件（客户端构建产物）
app.use(express.static(path.join(__dirname, '../client/build')));

// 所有路由都返回 index.html（用于 React Router）
app.get('*', (req, res) => {
    res.sendFile(path.join(__dirname, '../client/build/index.html'));
});
```

#### 步骤8：使用 PM2 管理进程（推荐）
```bash
# 安装 PM2
npm install -g pm2

# 启动服务器
cd /var/www/gomoku-online/server
pm2 start index.js --name gomoku-server

# 设置开机自启
pm2 startup
pm2 save
```

#### 步骤9：配置防火墙
```bash
# Ubuntu/Debian
sudo ufw allow 5000/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=5000/tcp
sudo firewall-cmd --reload
```

---

### 方案2：分离部署（客户端和服务器分开）

#### 客户端部署（可选：使用 Nginx）
```nginx
# /etc/nginx/sites-available/gomoku-client
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/gomoku-online/client/build;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

#### 服务器部署
```bash
# 只部署 server 目录
cd /var/www/gomoku-online/server
npm install --production
pm2 start index.js --name gomoku-server
```

**注意：** 如果客户端和服务器分离，需要配置 CORS：
```javascript
// server/index.js
const cors = require('cors');
app.use(cors({
    origin: 'http://your-client-domain.com', // 客户端域名
    credentials: true
}));
```

---

## 🔧 环境变量配置

### 服务器环境变量（server/.env）
```env
NODE_ENV=production
PORT=5000
```

### 客户端环境变量（client/.env.production）
```env
REACT_APP_SERVER_URL=http://your-server-ip:5000
# 或
REACT_APP_SERVER_URL=https://your-domain.com
```

然后修改代码使用环境变量：
```javascript
const ENDPOINT = process.env.REACT_APP_SERVER_URL || 'http://localhost:5000';
```

---

## 🔒 使用 HTTPS（推荐生产环境）

### 使用 Nginx 反向代理 + SSL
```nginx
# /etc/nginx/sites-available/gomoku
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # 客户端
    location / {
        root /var/www/gomoku-online/client/build;
        try_files $uri $uri/ /index.html;
    }

    # Socket.io 代理
    location /socket.io/ {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 📊 监控和维护

### 查看服务器状态
```bash
# PM2 状态
pm2 status

# 查看日志
pm2 logs gomoku-server

# 重启服务器
pm2 restart gomoku-server
```

### 更新部署
```bash
# 1. 拉取最新代码
git pull

# 2. 重新安装依赖（如果有新依赖）
cd server && npm install --production
cd ../client && npm install && npm run build

# 3. 重启服务器
pm2 restart gomoku-server
```

---

## 🐛 常见部署问题

### 问题1：Socket.io 连接失败
**原因：** CORS 配置或代理配置问题

**解决：**
1. 检查服务器 CORS 配置
2. 检查防火墙是否开放端口
3. 检查 Nginx 代理配置（如果使用）

### 问题2：静态文件404
**原因：** 路径配置错误

**解决：**
1. 检查 `express.static` 路径是否正确
2. 确保 `client/build` 目录存在
3. 检查文件权限

### 问题3：路由刷新404
**原因：** React Router 需要服务器支持

**解决：**
添加通配符路由返回 `index.html`：
```javascript
app.get('*', (req, res) => {
    res.sendFile(path.join(__dirname, '../client/build/index.html'));
});
```

### 问题4：端口被占用
**解决：**
```bash
# 查找占用进程
lsof -i :5000  # Linux/Mac
netstat -ano | findstr :5000  # Windows

# 结束进程或修改端口
```

---

## 📝 部署检查清单

- [ ] 服务器依赖已安装
- [ ] 客户端已构建（`npm run build`）
- [ ] 环境变量已配置
- [ ] 服务器地址已更新
- [ ] 防火墙端口已开放
- [ ] PM2 进程已启动
- [ ] 可以访问客户端页面
- [ ] Socket.io 连接正常
- [ ] 可以创建房间
- [ ] 可以加入房间
- [ ] 游戏可以正常进行

---

## 🎯 快速部署脚本

创建 `deploy.sh`：
```bash
#!/bin/bash

echo "开始部署..."

# 安装依赖
cd server && npm install --production
cd ../client && npm install && npm run build

# 重启服务
cd ../server
pm2 restart gomoku-server || pm2 start index.js --name gomoku-server

echo "部署完成！"
```

使用：
```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 📞 需要帮助？

如果部署过程中遇到问题：
1. 查看服务器日志：`pm2 logs gomoku-server`
2. 查看浏览器控制台错误
3. 检查网络连接和防火墙设置
4. 参考 `启动指南.md` 中的故障排除部分

---

**祝你部署顺利！** 🚀
